# Rules — frontend/public/

- Do NOT place files here that are imported via JSX — those belong in `src/assets/`
- Do NOT place files here that contain secrets or environment-specific config
- Keep this directory minimal — only files that must be URL-accessible
- `index.html` is the only HTML file; do not add additional HTML files
